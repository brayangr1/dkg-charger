import React from 'react';
import MainLayout from '@components/layout/MainLayout';

const PublicMapScreen: React.FC = () => {
    return (
        <MainLayout>
            <div style={{ padding: '20px' }}>
                <h1>Mapa de Cargadores Públicos</h1>
                <p>Esta pantalla se implementará en la Fase 5</p>
            </div>
        </MainLayout>
    );
};

export default PublicMapScreen;
